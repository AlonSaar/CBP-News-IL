# Secrets
.env
*.service-account.json
service-account-*.json
credentials.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# State — commit the data files, ignore nothing important
# (the bot needs to read/write these on every run)
!state/articles.json
!state/processed_urls.json
!state/last_run.json

# Logs
logs/*
!logs/.gitkeep

# Output — commit the generated files so GitHub Pages stays up to date
# output/ is intentionally NOT ignored

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
